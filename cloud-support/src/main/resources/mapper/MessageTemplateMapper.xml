<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duang.support.support.mapper.MessageTemplateMapper">

    <select id="get" resultType="com.duang.support.support.domain.MessageTemplate">
        SELECT * FROM user WHERE id = #{id}
    </select>

</mapper>